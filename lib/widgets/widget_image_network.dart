import 'package:flutter/material.dart';

class WidgetImageNewwork extends StatelessWidget {
  const WidgetImageNewwork({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}